{
  "routes": [
    {
      "method": "PATCH",
      "path": "/user/set-details",
      "handler": "user.setDetails",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PATCH",
      "path": "/user/set-preferences",
      "handler": "user.setPreferences",
      "config": {
        "policies": []
      }
    }
  ]
}
