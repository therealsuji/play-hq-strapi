{
  "routes": [
    {
      "method": "POST",
      "path": "/user/set-location",
      "handler": "user.setLocation",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/user/set-preferences",
      "handler": "user.setPreferences",
      "config": {
        "policies": []
      }
    }
  ]
}
