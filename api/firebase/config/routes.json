{
  "routes": [
    {
      "method": "POST",
      "path": "/firebase/auth",
      "handler": "firebase.auth",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/firebase/update-notification/:token",
      "handler": "firebase.updateFcmToken",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/firebase/renew-token",
      "handler": "firebase.renewToken",
      "config": {
        "policies": []
      }
    }
  ]
}
